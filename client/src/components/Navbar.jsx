import React from 'react';

const Navbar = () => {
  return (
    <div>
        PokeDex
        <button>Favorite Pokemon</button>
        <button>Logout</button>
    </div>
  )
}

export default Navbar;